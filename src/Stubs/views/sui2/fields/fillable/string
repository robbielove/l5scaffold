<div class="four wide column">
   <h4><i class="{{field.name}} icon"></i>{{field.Name}}</h4>
</div>
<div class="twelve wide column @if($errors->has('{{field.name}}')) error @endif field">
  <div class="ui fluid corner labeled input">
    <input type="text" id="{{field.name}}-field" name="{{field.name}}" class="ui input" value="{{ old('{{field.name}}', ${{model}}->{{field.name}} ) }}" placeholder="{{field.Name}}"/>
    <div class="ui corner label">
      <i class="asterisk icon"></i>
    </div>
  </div>
</div>
