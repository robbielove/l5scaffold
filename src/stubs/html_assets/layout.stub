<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="icon" href="favicon.ico">

    <title>Starter Template</title>
    <!-- Styles -->
        <link rel="stylesheet" type="text/css" class="ui" href="https://cdn.jsdelivr.net/semantic-ui/2.1.8/semantic.min.css">
        {{-- <link href="{{ elixir('css/app.css') }}" rel="stylesheet"> --}}
    {{-- CSS --}}
            <link rel="stylesheet" href="{{ elixir('css/vendor.css') }}">
            <link rel="stylesheet" href="{{ elixir('css/app.css') }}">

    @yield('css')
</head>

<body id="app-layout">
  <div class="ui inverted menu">
        <a class="active item" href="{{ url('/') }}"><i class="map icon"></i>Project Name</a>
        <a class="active blue item" href="{{ URL::to('/') }}"><i class="tasks icon"></i>Objects</a>
        <!-- Authentication Links -->
        @if (Auth::guest())
            <a class="ui right item" href="{{ url('/login') }}"><i class="lock icon"></i>Login</a>
            <a class="ui item" href="{{ url('/register') }}"><i class="add user icon"></i>Register</a>
        @else
            <div class="ui right item"><i class="user icon"></i>{{ Auth::user()->name }}</div>
            <div class="ui active red item"><a class="" href="{{ url('/logout') }}"><i class="unlock icon"></i>Logout</a></div>
        @endif
  </div>

    <div class="ui padded container">
        @yield('header')
        @yield('content')
    </div><!-- /.container -->

    <script src="https://cdn.jsdelivr.net/semantic-ui/2.1.8/semantic.min.js"></script>
    {{-- JS --}}
          <script src="{{ elixir('js/vendor.js') }}"></script>
          <script src="{{ elixir('js/app.js') }}"></script>

    @yield('scripts')
</body>
</html>
