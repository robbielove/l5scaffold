@extends('layouts.app')

@section('header')
    <div class="ui header">
      <h1><i class="big unhide icon"></i>{{Class}} / View</h1>
    </div>
@endsection

@section('content')
    <div class="ui two column very padded stackable grid">
        <div class="four wide column">
          <h4><i class="{{class}} icon"></i>{{ClassSingle}} Name</h4>
        </div>
        <div class="twelve wide column">
          <h4>{{${{classSingle}}->name}}</h4>
        </div>
        <div class="four wide column">
          <h4><i class="user icon"></i>Author</h4>
        </div>
        <div class="twelve wide column">
          <a class="ui mini label" href="{{ route('users.public', [${{classSingle}}->Author->slug]) }}"><i class="user icon"></i>{{${{classSingle}}->Author->name}}</a>
        </div>
        <div class="four wide column">
          <h4><i class="tag icon"></i>ID</h4>
        </div>
        <div class="twelve wide column">
          <h5><div class="ui mini tag label">{{${{classSingle}}->id}}</div></h5>
        </div>
        <div class="four wide column">
          <h4><i class="linkify icon"></i>Slug</h4>
        </div>
        <div class="twelve wide column">
          <h4><a href="{{ ${{classSingle}}->slug }}" target="_blank">{{ route('{{prefix}}{{class}}.show', [${{classSingle}}->slug]) }}</a></h4>
        </div>
        <div class="four wide column">
          <h4><i class="write icon"></i>Created</h4>
        </div>
        <div class="twelve wide column">
          <h5><div class="ui mini label"><i class="write icon"></i>{{ date('j F Y', strtotime(${{classSingle}}->created_at)) }}<i class=" icon"></i> <i class="time icon"></i>{{ date('g:i a', strtotime(${{classSingle}}->created_at)) }}</div></h5>
        </div>
        <div class="four wide column">
          <h4><i class="edit icon"></i>Updated</h4>
        </div>
        <div class="twelve wide column">
          <h5><div class="ui mini label"><i class="edit icon"></i>{{ date('j F Y', strtotime(${{classSingle}}->updated_at)) }}<i class=" icon"></i> <i class="time icon"></i>{{ date('g:i a', strtotime(${{classSingle}}->updated_at)) }}</div></h5>
        </div>
        <div class="four wide column">
          <h4><i class="align left icon"></i>Description</h4>
        </div>
        <div class="twelve wide column">
          <p>{!! nl2br(${{classSingle}}->description) !!}</p>
        </div>
    </div>
    {{content_fields}}
    <div class="ui divider"></div>
    <div class="ui basic segment">
      <div class="ui three column stackable grid">
        <div class="column">
          <a class="ui fluid teal button" href="{{ route('{{prefix}}{{class}}.edit', [${{classSingle}}->slug]) }}"><i class="edit icon"></i>Edit</a>
        </div>
        <div class="column">
          <form class="right" action="{{ route('{{prefix}}{{class}}.destroy', [${{classSingle}}->slug]) }}" method="POST" style="display: inline;" onsubmit="if(confirm('Delete? Are you sure?')) { return true } else {return false };">
            <input type="hidden" name="_method" value="DELETE">
            <input type="hidden" name="_token" value="{{ csrf_token() }}">
            <button type="submit" class="ui fluid button"><i class="hide icon"></i>De-Activate</button>
          </form>
        </div>
        <div class="column">
          <a class="ui fluid blue button" href="{{ route('{{prefix}}{{class}}.index') }}"><i class="undo icon"></i>Back to all</a>
        </div>
      </div>
    </div>
@endsection
